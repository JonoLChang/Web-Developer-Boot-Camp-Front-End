var ques = prompt("Are we there yet?");

var lowerQues = ques.toLowerCase

while(lowerQues != ("yes" || "yeah")) {
    if(lowerQues === ("yes" || "yeah")) {
        alert("Yay, we finally made it!")
    }
}